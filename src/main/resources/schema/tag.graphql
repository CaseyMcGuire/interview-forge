extend type Query {
  """Tags ordered by display name, then slug."""
  tags: [Tag!]!
}

extend type Problem {
  """Assigned tags ordered by display name, then slug."""
  tags: [Tag!]!
}

type Tag {
  id: ID!

  """Stable identifier that cannot be changed after creation."""
  slug: String!

  displayName: String!
}
